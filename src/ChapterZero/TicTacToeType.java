package ChapterZero;

public enum TicTacToeType {
    EMPTY, X, O;

    private static int value;
    TicTacToeType() {

    }

    /*
    create a class TicTacToe
    TicTacToe has a board
    enum X,O,EMPTY
    constructor to initialize the board element as EMPTY
    enum class two players
    player1 move is == X
    player@ move is == O


     */
}
